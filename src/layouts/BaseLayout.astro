---
import '../styles/global.css';
import { defaultLanguage, useTranslations, type LanguageCode } from '@/i18n/ui';
import Footer from '@/components/Footer.astro';
import Header from '@/components/Header.astro';
import { ClientRouter } from 'astro:transitions';
import { Toaster } from 'sonner';

type Props = {
  pageTitle: string;
  pageDescription?: string;
  ogImage?: string;
  ogType?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
};

export type NavItem = {
  href: string;
  label: string;
};

const {
  pageTitle,
  pageDescription,
  ogImage,
  ogType = 'website',
  publishedTime,
  modifiedTime,
  author,
} = Astro.props;
const lang =
  (Astro.currentLocale as LanguageCode | undefined) ?? defaultLanguage;

const tSite = useTranslations(lang, 'site');
const tNav = useTranslations(lang, 'nav');

const description = pageDescription || tSite('description');
const siteOrigin = Astro.site?.toString().replace(/\/$/, '') ?? '';
const ogImageUrl = ogImage
  ? (ogImage.startsWith('http') ? ogImage : `${siteOrigin}${ogImage.startsWith('/') ? '' : '/'}${ogImage}`)
  : `${siteOrigin}/og-default.svg`;
const canonicalUrl = `${siteOrigin}${Astro.url.pathname}`;

const navItems: Array<NavItem> = [
  { href: '/', label: tNav('home') },
  { href: '/blog', label: tNav('blog') },
  { href: '/about', label: tNav('about') },
  { href: '/contact', label: tNav('contact') },
];

const siteGraph = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': 'https://myneatflow.com/#organization',
      name: 'NeatFlow',
      url: 'https://myneatflow.com',
      logo: 'https://myneatflow.com/favicons/light-mode/web-app-manifest-512x512.png',
      founder: { '@id': 'https://myneatflow.com/#paulo' },
      sameAs: [
        'https://github.com/Pauloa90',
        'https://www.instagram.com/pauloandre90/',
      ],
    },
    {
      '@type': 'Person',
      '@id': 'https://myneatflow.com/#paulo',
      name: 'Paulo Albuquerque',
      url: 'https://myneatflow.com/about',
      jobTitle: 'Founder',
      worksFor: { '@id': 'https://myneatflow.com/#organization' },
      knowsAbout: [
        'AI automation',
        'n8n workflows',
        'Smart billing reminders',
        'Business process automation',
      ],
      sameAs: [
        'https://github.com/Pauloa90',
        'https://www.linkedin.com/in/paulo-albuquerque-2a56673b/',
      ],
    },
    {
      '@type': 'WebSite',
      '@id': 'https://myneatflow.com/#website',
      url: 'https://myneatflow.com',
      name: 'NeatFlow',
      publisher: { '@id': 'https://myneatflow.com/#organization' },
    },
  ],
};
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="google-site-verification" content="Okah5bP5KxbmeIFZycXb6kVm630GBmOTmkQtVl5ChfI" />
    <meta name="msvalidate.01" content="411D2AD8A2FDC51ED895EAF8CA56CD41" />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate" type="application/rss+xml" title="Paulo Albuquerque — RSS" href="/rss.xml" />

    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={pageTitle || tSite('title')} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content="Paulo Albuquerque" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle || tSite('title')} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    {ogType === 'article' && publishedTime && (
      <meta property="article:published_time" content={publishedTime} />
    )}
    {ogType === 'article' && modifiedTime && (
      <meta property="article:modified_time" content={modifiedTime} />
    )}
    {ogType === 'article' && (
      <meta property="article:author" content={author || 'Paulo Albuquerque'} />
    )}
    <script type="application/ld+json" set:html={JSON.stringify(siteGraph)} />
    <slot name="head" />
    <script is:inline>
      const getInitialTheme = () => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }
        if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
        }
        return 'light';
      };
      const theme = getInitialTheme();
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      }
      if (typeof localStorage !== 'undefined') {
        localStorage.setItem('theme', theme);
      }
    </script>
    <title>{pageTitle || tSite('title')}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <ClientRouter />
  </head>
  <body class="min-h-screen bg-background font-sans antialiased flex flex-col">
    <Header navItems={navItems} />
    <main class="container mx-auto px-4 py-8 flex-1 max-w-screen-2xl">
      <slot />
    </main>
    <Footer />
    <Toaster position="bottom-right" richColors client:load />
  </body>
</html>
